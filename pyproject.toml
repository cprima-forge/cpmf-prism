[project]
name = "cpmf-prism"
version = "0.1.0"
description = "Tools for the CPMF Prism VS Code extension"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "azure-devops>=7.1.0b4",
    "azure-identity>=1.25.3",
    "python-dotenv>=1.2.2",
    "ruff>=0.9.0",
]
